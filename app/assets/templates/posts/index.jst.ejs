<ul>
  <% _.each(posts, function (post) {%>
    <li>
      <%= post.title %>
      <%= post.body %>
      <button class='button-delete' data-id='<%= post.id %>' value='Delete'>
        Delete
      </button>
    </li>
  <% }); %>
</ul>