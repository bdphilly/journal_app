<ul>
  <% posts.each(function (post) {%>
    <li>
      <%= post.get('title') %>
      <%= post.get('body') %>
      <button class='button-delete' data-id='<%= post.id %>' value='Delete'>
        Delete
      </button>
    </li>
  <% }); %>
</ul>